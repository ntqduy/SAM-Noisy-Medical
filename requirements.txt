# Core dependencies
numpy>=1.21.0
pillow>=9.0.0
tqdm>=4.62.0
pyyaml>=6.0
matplotlib>=3.5.0
pandas>=1.3.0
reportlab>=3.6.0
scipy>=1.7.0
opencv-python>=4.5.0
torchvision>=0.11.0
nibabel>=4.0.0

# Optional: PyTorch (required for SAM models)
# Install PyTorch according to your CUDA version:
# https://pytorch.org/get-started/locally/
# torch>=1.10.0
# torchvision>=0.11.0

# Optional: Segment Anything Model (SAM)
# pip install git+https://github.com/facebookresearch/segment-anything.git

# Optional: SAM2
# pip install git+https://github.com/facebookresearch/segment-anything-2.git

# Optional: MedSAM (uses same package as SAM)
# Download weights from: https://github.com/bowang-lab/MedSAM
